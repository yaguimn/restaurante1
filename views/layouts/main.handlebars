<!DOCTYPE html>
<html lang="pt-br">
<head>
   <meta charset="UTF-8" />
   <meta name="viewport" content="width=device-width, initial-scale=1" />
   <title>Base</title>
   <style>
      /* Reset básico */
      * {
         margin: 0;
         padding: 0;
         box-sizing: border-box;
      }

      body {
         font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
         background: linear-gradient(135deg, #6b8dd6, #3a5bbf);
         min-height: 100vh;
         display: flex;
         flex-direction: column;
         align-items: center;
         justify-content: center;
         padding: 30px 15px;
         color: #f0f4f8;
      }

      h1, h2 {
         text-align: center;
         margin-bottom: 20px;
         color: #f0f4f8;
         text-shadow: 1px 1px 4px rgba(0,0,0,0.4);
      }

      .card {
         background: rgba(255, 255, 255, 0.15);
         padding: 30px 25px;
         width: 90%;
         max-width: 650px;
         margin: 25px auto;
         border-radius: 15px;
         border: 1.5px solid rgba(255, 255, 255, 0.3);
         box-shadow: 0 8px 20px rgba(0,0,0,0.25);
         backdrop-filter: blur(10px);
         transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
         text-align: center;
      }

      .card:hover {
         transform: translateY(-6px) scale(1.04);
         box-shadow: 0 12px 25px rgba(0,0,0,0.35);
      }

      ul {
         list-style: none;
         padding: 0;
         margin-top: 20px;
      }

      li a {
         display: block;
         padding: 14px 20px;
         margin: 8px 0;
         text-decoration: none;
         color: #142850;
         font-weight: 600;
         border-radius: 10px;
         background: #f0f4f8;
         box-shadow: 2px 2px 6px rgba(20, 40, 80, 0.15);
         transition: background 0.3s ease, color 0.3s ease, box-shadow 0.3s ease;
      }

      li a:hover {
         background: #1f4068;
         color: #f0f4f8;
         box-shadow: 3px 3px 10px rgba(31, 64, 104, 0.7);
      }

      #titulo {
         font-size: 1.8rem;
         font-weight: 700;
         padding-bottom: 12px;
         border-bottom: 3px solid #f0f4f8;
         margin-bottom: 25px;
         letter-spacing: 1.1px;
         text-transform: uppercase;
      }
   </style>
</head>
<body>
   {{>header}}

   <div class="card">
      <h1 id="titulo">Bem-vindo ao Cardápio</h1>
      {{{body}}}
   </div>
</body>
</html>
